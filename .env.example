# Copy this file to `.env` and replace the <db_password> placeholder with your MongoDB password.
# If the password contains special characters (like @, /, :), URL-encode it first.

PORT=5000
MONGO_URI=mongodb+srv://irfan14:<db_password>@fyntrix.jieq8ha.mongodb.net/?appName=Fyntrix
JWT_SECRET=your_super_secret_key
JWT_EXPIRES_IN=90d
